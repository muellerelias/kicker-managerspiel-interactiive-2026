// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build/config
export default defineConfig({
  site: 'https://muellerelias.github.io',
  base: '/kicker-managerspiel-interactiive-2026',
});
